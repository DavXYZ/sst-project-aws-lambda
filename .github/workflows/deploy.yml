name: Deploy SST

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:
    inputs:
      stage:
        description: 'Stage to deploy to'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - staging
          - production

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.stage || github.ref == 'refs/heads/main' && 'production' || github.ref == 'refs/heads/develop' && 'staging' || 'staging' }}
    concurrency:
      group: sstproject-deploy-${{ github.event_name == 'workflow_dispatch' && github.event.inputs.stage || github.ref == 'refs/heads/main' && 'production' || github.ref == 'refs/heads/develop' && 'staging' || 'staging' }}
      cancel-in-progress: false
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
      STAGE: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.stage || github.ref == 'refs/heads/main' && 'production' || github.ref == 'refs/heads/develop' && 'staging' || 'staging' }}
      AWS_REGION: ${{ vars.AWS_REGION || 'us-east-1' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 'lts/*'

      - name: Show Node and npm versions
        run: |
          node --version
          npm --version

      - name: Determine stage
        id: stage
        run: |
          echo "stage=$STAGE" >> "$GITHUB_OUTPUT"
          echo "Deploying to stage: $STAGE"

      - name: Install root dependencies
        run: npm install

      - name: Install web dependencies
        working-directory: web
        run: npm install

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v5
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}


      - name: Deploy to ${{ steps.stage.outputs.stage }}
        shell: bash
        run: |
          set -o pipefail
          LOG_FILE="$(mktemp)"

          echo "Starting SST deploy for stage '${{ steps.stage.outputs.stage }}'"
          mkdir -p .sst/log
          : > .sst/log/pulumi.log
          : > .sst/log/sst.log

          if npx sst deploy --stage "${{ steps.stage.outputs.stage }}" --print-logs 2>&1 | tee "$LOG_FILE"; then
            echo "SST deploy completed successfully"
            exit 0
          fi

          if [ -d .sst/log ]; then
            echo "----- .sst/log contents -----"
            ls -la .sst/log
            echo "----- end .sst/log contents -----"

            shopt -s nullglob
            for file in .sst/log/*.log; do
              echo "----- $file -----"
              cat "$file"
              echo "----- end $file -----"
            done
          fi

          if grep -q "A concurrent update was detected on the app" "$LOG_FILE"; then
            echo "Detected stale SST lock. Unlocking stage '${{ steps.stage.outputs.stage }}' and retrying once..."
            npx sst unlock --stage "${{ steps.stage.outputs.stage }}"
            npx sst deploy --stage "${{ steps.stage.outputs.stage }}" --print-logs
            exit 0
          fi

          if grep -q "NoSuchDistribution" "$LOG_FILE"; then
            echo "Detected stale CloudFront reference in SST state. Running sst refresh to reconcile state with AWS and retrying..."
            npx sst refresh --stage "${{ steps.stage.outputs.stage }}"
            npx sst deploy --stage "${{ steps.stage.outputs.stage }}" --print-logs
            exit 0
          fi

          echo "SST deploy failed. See log above."
          exit 1
        env:
          SST_LOG: debug
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY }}
          STRIPE_PRICE_ID: ${{ secrets.STRIPE_PRICE_ID }}
          STRIPE_WEBHOOK_SECRET: ${{ secrets.STRIPE_WEBHOOK_SECRET }}
          STRIPE_PUBLISHABLE_KEY: ${{ secrets.STRIPE_PUBLISHABLE_KEY }}
          APP_URL: ${{ vars.APP_URL }}
          VITE_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
